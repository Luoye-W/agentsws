# packages/adapter

@agentsws/dsh-adapter —— 唯一允许 import @deepseek-ai/dsh-* 的包。封装 tools/pre-execute、tools/post-execute、ctx.approval answerer、systemPrompt.section/context、ctx.tools.restrict、SDK client、preset 挂载。每个 seam 一组契约测试；升级 dsh 只看这里。
