# 54 · 岗位是任务主入口：岗位与职责各有上下文与记忆 v1

| | |
|---|---|
| 状态 | 2026-09-15 Luoye 定原则；本文把它落成对象与落点，**按第一版判断直接派 WP69 实现**，Luoye 看完可改；**2026-09-15 WP69 实现完成，待审**（§5 落点已补成实际文件）；**2026-09-16 WP70 完成，待审**（§4 最后一条：职责一律归岗位、默认折叠收纳） |
| 原则（Luoye 原话） | 开启任务主要在**岗位**里开启，不然分得太细、用户认知和使用成本太高；保留从**职责**里开启任务；岗位和职责都有对应的上下文、记忆 |
| 关联 | 05（Role / Position / Assignment：岗位只在分配那一刻展开——**本文给岗位加运行时实体**）、37 §2.2b（事项 = 上下文的家）、41（秘书路由到岗位）、23 / 技能层（package / company / department / personal）、19（知识）、48 §5（红人五渠道职责——第一个按本文建的岗位） |

## 0. 一句话

用户面对的是**岗位**（客服、网站运营、红人营销……），不是职责。开一件事 = 交给一个岗位；岗位里的 Agent 自己判断该走哪条职责、用哪条职责的额度与权限去做（**路由，不是权限并集**——05 §4 的"不做跨 Assignment 并集"不变）。职责入口保留给"我就要它用售后那条规则来做"的人。岗位与职责各有一层记忆：岗位记的是"这个岗位在这家公司怎么做事"，职责记的是"这条活儿的专业教训"。

## 1. 对象（Q1）

```
Position（岗位模板，05 §2，不变）
└─ PositionInstance（岗位实体，新）：一个工作区里"网站运营"这个岗位本身
     ├─ holders：谁在做（算出来的：名下有该岗位默认职责全集的人，05 §2 原则不变）
     ├─ roles：展开的职责 → 各自的 Assignment（已有）
     ├─ memory：岗位记忆（技能层 `position` + 教训池里提升到岗位层的条目）
     └─ matters：`position_id` 挂这个岗位的事项（37 的 Matter 已有 `position_id?`）
Role（职责，05 §1，不变）
     └─ memory：职责记忆（技能层 `role` + 提升到职责层的教训）
```

- `Matter` 加 `entry: 'position' | 'role'`（从哪儿开的）与 `role_id?`（职责入口时指定；岗位入口时由路由填、可换）。
- 技能层从四层变六层：`package → company → department → position → role → personal`（越靠后越具体、越优先；只加不删，现有四层的语义与文件一个不动）。
- 教训（learning）提升目标加 `position` / `role` 两档：一条教训可以从个人层提到"这个岗位"或"这条职责"，同岗位的其他人 / 同职责的其他分配都吃到。

## 2. 入口（Q2）

| 入口 | 谁用 | 做什么 |
|---|---|---|
| **岗位**（主） | 所有人 | 首页岗位卡与岗位页顶部一个按钮"交给这个岗位一件事"：一句话 + 可选附件 / 关联订单 → 开事项（`entry: position`）→ 岗位路由挑职责 → 起 Run（用那条职责的 Assignment、额度、技能）→ 卡片回到事项时间线 |
| 职责（次） | 想指定规则的人 | 岗位页里职责那一层每条职责旁一个小按钮"用这条职责开"（`entry: role`），跳过路由 |
| 秘书路由（41） | 谁都可以丢一句话 | 已经路由到**岗位**；出的认领卡认领后 = 岗位入口 |
| 卡片 / 来信 / 定时 | 系统 | 不变（它们本来就带职责） |

岗位路由 = 41 秘书路由那套判据（职责名 / 描述短语 / grounding 意图词 / 动作 id / 数据域）在**一个岗位的职责集合内**跑一遍；拿不准就问一句（一张选择卡：这件事像 A 也像 B，你定），不猜。路由结果写在事项时间线上，可以手动换职责。

## 3. 上下文与记忆（Q3）

| 层 | 上下文（起 Run 时喂给模型的） | 记忆（跨事项留下来的） |
|---|---|---|
| 岗位 | 岗位面板的数字块与告警（36）、岗位下所有职责的进行中事项摘要、岗位持有人的可用时段 | `position` 技能层（"这家公司的网站运营改价先看竞品"）、提升到岗位层的教训 |
| 职责 | 职责 yml（scopes / 动作 / 额度 / grounding）、职责技能、知识适用范围（19）、该职责的历史事项 | `role` 技能层、提升到职责层的教训 |
| 个人 | 本人 profile、可用时段 | 个人记忆与 `personal` 技能层（不变，21 的敏感级不变） |

组装顺序 = 六层叠加（§1）；岗位入口的 Run 同时带岗位层与被路由到的那条职责层；职责入口的 Run 也带岗位层（它仍属于那个岗位）。记忆写入：事项收尾时的复盘（已有）可以把一条教训**提**到岗位层或职责层，走 learning 的提议 → 批准那条路，不自动写。

**手改入口（WP71，36 §10）**：除了"复盘提升"这条自动路，每一层的记忆还能**手改**——第三栏的「记忆」面板里，本层的条目可改 / 可删 / 可手动加一条，也可以"提到上一层"。三条规矩分得很清：

| 动作 | 走哪条路 | 为什么 |
|---|---|---|
| 改 / 删 / 手动加**本层**的一条 | `POST` / `PATCH` / `DELETE /v1/memory` —— **当场写** | 本层就是本人自己干活的那一层，没有第二个人要为它点头 |
| **提到上一层** | `POST /v1/skills/:name/promote` → 一张待审卡 | 上一层是别人也要吃的东西（24 §3 不批不生效） |
| 复盘提升 | 同上（已有） | 不变 |

改得动哪一层看**你在哪一层干活**：职责层要本人持有该职责，岗位层要本人在这个岗位下至少持有一条职责，公司 / 部门层要 owner，包层与个人层这条路不开。判据在服务端一份（`canEditMemory`），`GET /v1/memory` 回一格 `can_edit` 给界面，界面不自己判第二遍。手动加的那一条落在**本层自己的技能记录**里（不是 overlay），所以它会被下一次 `resolve` 并进去、真的喂给模型。

## 4. 认知成本的硬规矩（Q4）

- 首页只有岗位，没有职责；职责只在岗位页展开的第二层看得见。
- 一个人只有一个岗位：首页直接就是那个岗位页。
- 首次设置（46）勾的是岗位，职责默认全勾在岗位里，不单独列一页。
- 通知与卡片按岗位聚合（"网站运营：3 张待审"），点开才看到是哪条职责的。
- **职责一律归在岗位下面，默认折叠收纳**（09-16 Luoye）：左侧导航、公司页、人员页、成员页、⌘K 一律先岗位后职责，职责折叠着、标题旁只有一个条数；一个岗位只有一条职责时不出第二层。公司页不再有独立的"职责"tab。→ WP70。
- **职责一律归岗位、默认折叠（WP70，2026-09-16 Luoye 原话："职责应该归属于岗位下面，
  默认折叠收纳状态"）**：任何列出"人在做什么 / 这家公司有什么"的地方，一律先岗位、
  后职责；职责默认折叠，标题旁一个数字（"3 条职责"），点开才展开；**一个岗位只有
  一条职责时不折叠**——直接把那一条的名字当岗位的副标题，不再出第二层（多一层折叠
  去看一条，白花认知成本）。

  | 落在哪儿 | 折叠前 | 折叠后 |
  |---|---|---|
  | 左栏「岗位」栏 | 一条分配一行（= 职责，做网站运营的人看到四行） | 一个岗位一行 + 待审数角标；点进去是本人在这个岗位下的第一条分配 |
  | 当前分配（`X-Assignment`） | 跟着左栏点的那条职责走 | 跟着**岗位**走：已经属于这个岗位就不动它，职责层的切换只在岗位页折叠层里 |
  | 公司页 | 顶层一个独立的「职责」tab + 岗位卡上摊开一排职责徽章 | **没有「职责」tab 了**；岗位一行（岗位名 / 持有人 / 职责数），折叠层里每条职责带原「职责」tab 的动作（复制一份 / 改名与额度 / 已提交审批） |
  | 成员 tab、人员页、同事 profile | 每人一串职责徽章 | 先岗位徽章，职责折在下面；归不进岗位的历史数据归成「未归岗位 · N 条职责」 |
  | ⌘K | 按分配列（= 职责） | 岗位名排在前；职责也搜得到，那一行显示成「岗位 › 职责」 |
  | 首次设置第 ③ 步（46） | 本来就是岗位在外、职责折叠 | 不动，只把折叠件抽成共用的一份 |

  一份实现：`apps/workstation/src/components/ui/duty-fold.tsx`（折叠件）+
  `lib/positions.ts`（`assignmentForPosition` / `groupDutiesByPosition`）+
  `components/work/duty-badges.tsx`（人那一侧的岗位徽章）。没装岗位面的服务进程
  （`GET /v1/positions` 不回 `instances`）一律退回现状，契约与路由一个字没动。
  截图：`docs/assets/workstation/{nav-positions-folded,org-positions-folded}.png`。

## 5. 落点（WP69 已实现；红人岗位 WP67 直接按本文建）

| 层 | 改什么 | 实际文件 |
|---|---|---|
| 契约 | `Matter.entry` / `role_id?` / `position_template_id?`（三个都可选，**存量事项一个字段不多**）；`SkillTier` 四层 → 六层；`PromotionTier` / `PromotionTarget` 四档；`PositionInstance`（见 05 §2.1）；`ObjectType` 加 `position`；事件 `matter.routed` / `matter.rerouted` | `packages/contracts/src/{work,skills,roles,common,events}.ts` |
| 判据 | **判据抽成一份**：`routeWithinPosition(text, roles[]) → { picked?, candidates, ambiguous, reason }`；秘书路由改调它，行为一个字没变 | `packages/roles/src/route.ts`（原 `packages/secretary/src/route.ts`） |
| 服务 | 岗位实体、岗位内路由（**只在请求人自己名下的职责里挑**）、选择卡、`reroute`、岗位层上下文三样 | `apps/server/src/positions.ts` |
| 六层技能 | `TIER_ORDER` 六层；`position` / `role` 两层按 scope 取；目录约定 `skills/positions/<id>`、`skills/roles/<id>` | `packages/skills/src/registry.ts`、`apps/server/src/runtime.ts`、`packages/learning/src/resolved.ts` |
| 记忆 | `promote` 目标四档（提到岗位 / 职责层要 `scope_id`）；`memoryAt` / `memorySummary` | `apps/server/src/learning.ts` |
| API | `GET /v1/positions/:id`、`POST /v1/positions/:id/matters`、`POST /v1/matters/:id/reroute`、`GET /v1/memory`；`GET /v1/positions` 多带一份 `instances`；`POST /v1/skills/:name/promote` 加两档与 `scope_id` | `packages/api/src/routes/{positions,workstation,skills}.ts` |
| 工作台 | 首页岗位卡（职责不出现、计数按岗位聚合、单岗位直落岗位页）；岗位页顶部「交给这个岗位一件事」+ 折叠的职责层 + 「记忆」tab；事项页「路由到 X · 换」 | `apps/workstation/src/{pages/{home,position,matter}.tsx,components/work/{position-entry,layer-memory}.tsx,lib/{api,i18n}.ts}` |
| 模拟 | 15 人 pack 加 `org/task-opened-at-position-routes-to-duty`（15 → 16）；DSL 加 `position.staff` / `position.open` 与断言 `position_routed_to` | `packs/dtc-15p/scenarios/org/task-opened-at-position-routes-to-duty.yml`、`packages/simulation/src/{positions,runner,world,expectations,scenario/*}.ts` |
| 已有岗位 | 客服、网站运营两个岗位跟着机制走，模板一个字没动 | `packages/roles/positions/{customer-care,web-ops}.yml` |

### 5.1 阈值与叠加顺序（实现里定的两个数）

**岗位内路由**（`packages/roles/src/route.ts`）。候选分数是**归一后的份额**（这一条的
判据词权重之和 ÷ 参赛各条之和），于是两条阈值都读得懂：

| 线 | 值 | 为什么 |
|---|---|---|
| `MIN_SEPARATION` | 0.15 | 第一名与第二名的份额差。"六四开还算分得开、五五开就别猜了"——`0.575 : 0.425` 正好卡在线上 |
| `MIN_PICKED_SCORE` | 0.3 | 第一名自己的份额。四条职责平分（各 0.25）时谁也不像，这条把"哪条都沾一点"挡在外面 |

岗位下只有一条职责时两条都不看（没得选就是它）；一个判据词都没命中也算拿不准
（空手猜一条比问一句糟）。通用职责（`common.member` / `common.owner`）不参赛。

**六层叠加顺序**（`packages/skills/src/registry.ts` 的 `TIER_ORDER`，唯一一份真源）：

```
package → company → department → position → role → personal
```

越靠后越具体、越优先。职责压在岗位上面：一个岗位里有好几条职责，职责那一层说的话
更贴着这件活。`actor` 上没有 `position_id` / `role_id` 时那一层**自然被跳过**——
一条职责挂在多个岗位里、而分配上没记岗位时就是这种情形，这时候在事项时间线上说一句
（§3 最后一行），不猜一个。

### 5.2 两处与本文不同的地方（实现时定的）

1. **岗位与分配分了两个字段**。`Matter.position_id` 一直是 **Assignment id**（36 §3 的
   "岗位"= 本人持有的那一条），工作台、deck、日历都在读它。所以"这件事属于哪个岗位"
   另立一格 `position_template_id`——一个说"用谁的哪条分配在做"（权限与额度从它来），
   一个说"它属于哪个岗位"（岗位层上下文与记忆从它来）。合成一格就得改掉半个工作台。
2. **`GET /v1/positions/:id` 两种 id 都收**。同一前缀下 `/cards`、`/view`、`/ontology`
   里的 `:id` 是 assignment_id，而岗位实体讲的是岗位模板。为了不让界面被迫先查一次
   模板 id，这条路由两种都认（给的是分配就换算成它所属的岗位）。
