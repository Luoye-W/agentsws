# WP140 demo 修好：限流走墙钟、云账号替身、客服四条职责、测试剧本清掉、走查一次跑完

worktree `../agentsws-wt/wp140-demo` · 分支 `wp/140-demo`（从 main 新起）。先读 `_common.md`、`_beta-fix-common.md`、`docs/78` §1 #3、#6 最后一段、§2「首页」「客服」「通用」。

## 要做
1. **限流用墙钟**：`packages/api/src/app.ts` 约 229 行 `nowMs = Date.parse(deps.clock.now())` → 令牌桶按真实时间回血（它防真实世界刷接口，和合成时间无关）。加测试：合成时钟不动、墙钟过了窗口 → 额度恢复。真安装行为不变（本来就是系统时钟）。
2. **demo 里云账号一跳换替身**：demo（合成世界）点「发登录信」不许打生产云。替身回「信发出去了」，并能在 demo 里模拟「已关联」（有积分余额、价目三块、充值四档可见）。生产路径不变。
3. **demo 种子**：客服岗位四条职责都启用（不是只有两条，岗位页「4 条」与左栏一致）；红人岗位「5 条职责」与左栏一致（docs/66 #2 / #17）；「正在进行」里去掉测试剧本（「假指派：在场的人 + 高风险动作（转账）」），负责人不显示人员 id；首页右侧「Meta Ads」数据块只出一次。
4. **右栏四个「这个面板还没做」的面板**（数据面板、运行中、定时任务、文件）：内测期间**藏起来**（Fable 定）。用一个明确的开关（常量或特性标记），以后做好了一行放出来；不是删代码。
5. **走查脚本回到一次跑完**：去掉「每段起一个全新 demo」（保留 `--only`），第 51 步「demo 配额探针」改成验证「用完后过窗口能恢复」。
6. demo 的网址夹具认不带结尾斜杠的 `https://nordvolt.example`（docs/78 §2 向导第 ② 条最后一点）。

## 验收
- `node scripts/walkthrough-beta.mjs --port 4433`（**不带 --only**，一次起一个 demo 跑完五段）不出现「请求过于频繁」；第 51 步「通」。证据图到 `docs/assets/wp140/`。
- 新测试：墙钟限流、云账号替身不出网（断言没有对 cloud.agentsws.com 的请求）、四个面板被藏且开关能放出来。

## 验证（审核方全量用）
`vitest run packages/api apps/cli apps/server apps/workstation`；fast 模拟两个包三个运行时（种子若影响指标，按惯例 `--rewrite-baseline` 并在报告里写明哪些指标为什么变）。
