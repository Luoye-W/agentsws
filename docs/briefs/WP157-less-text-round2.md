# WP157 界面减字第二轮：连接页、消息渠道、浏览器插件卡、聊天窗、岗位与面板 + 两套 markdown 渲染合一

worktree `../agentsws-wt/wp157-less-text-2` · 分支 `wp/157-less-text-2`（从 main 新起）。先读 `_common.md`、docs/36 §7（WP156 改写后的三档规矩）、`docs/briefs/reports/WP156.md` 末节「下一轮要减字的地方清单」、
`apps/workstation/test/less-text.test.tsx`（守卫）、`apps/workstation/src/lib/help.ts` 与 `components/help/`（教程面板）、WP153 的时间线 markdown 渲染。

## Luoye 09-26 定（规矩同 WP156）
界面只留标题、一句话、操作、状态与结果；步骤进教程文章（右栏「教程」），短说明进问号；安全承诺、错误、状态、空态、会扣钱的提示一眼可见（压成一句）。**信息一条都不丢。**

## 要做
1. **连接页**：26 张连接卡（报告统计约 104 步、3600 字、40 个外链）全部按规矩改；每类连接一篇教程（能合并的合并，比如「Google 家的几个连接」一篇），中英各一份，外链全部搬进教程（沿用 WP156 的「外链都在教程里」服务端测试思路扩到连接目录）。
2. **消息渠道**：微信卡等；**浏览器插件卡**写死的中文改走 i18n 并减字；**聊天窗**转发三种方式那三句；MCP 与红人导入各一句进问号（WP156 报告点名的都要清）。
3. **岗位页与各职责面板**（客服、红人、网站运营、社媒、投放、设计、公关、建站）：顶上的说明段、空态以外的灰字按规矩减；WP154（内容与搜索）若已合进 main，它的新面板也一起减。
4. **两套 markdown 渲染合一**：WP153（时间线）与 WP156（教程）各写了一份，合成一个安全渲染组件（取两者之并：粗体、列表、编号、行内代码、链接只许 http(s) 且 noopener、表格、提示框、教程互链；不解析 HTML、不加载外部图片），两处都用它，测试合并。
5. **教程目录进 ⌘K**：命令面板里能搜教程标题，回车在右栏打开。
6. **守卫扩范围**：`less-text.test.tsx` 覆盖到连接页、消息渠道、岗位页与面板；上限沿用 60 / 40。
7. 截图前后对比成对放 `docs/assets/wp157/`；报告末节再列一次剩下没减的地方（应当很少）。

## 纪律
不联网、不用真账号；不跑批量清理命令；不读 .env*。Luoye 的本机服务在 4317，别碰。WP154 在并行（内容与搜索，改内容职责面板与设置），改共享文件只动你那几处，收尾 merge main 冲突两边都留。截图 demo 端口 4446；走查 `--only A,B,C` 不退化（改了点击目标就跟着改脚本）。

## 验证（审核方全量用）
`vitest run apps/workstation apps/server` + 走查。
