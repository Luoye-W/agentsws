# WP142 红人为主的朋友第一步：向导按主要目的预勾、第 ④ 步只列必需、找人回话有用、云账号一跳有反馈、没关联也看得到价

worktree `../agentsws-wt/wp142-kol-first` · 分支 `wp/142-kol-first`（从 main 新起，**等 WP138 合并后再起**——两边都动向导）。先读 `_common.md`、`_beta-fix-common.md`、`docs/78` §1 #5、#6、#7 与 §2「向导」。

## Fable 已定
- 向导第 ③ 步之前问一句「你这次主要想让它干什么」：红人营销 / 客服 / 都要（可多选，默认按网址预勾的结果）。按答案预勾岗位；红人营销从此能被预勾。

## 要做
1. **向导**：上面那一问；第 ④ 步只列**必需**的连接，可选的折成「还有 N 个可选」；技能包显示中文名（`brand-voice` 等不露）；完成屏的职责数与第 ④ 步同口径（已有的被跳过要说）；完成后不多出没勾过的「普通成员」岗位（若是设计如此，就在完成屏说一句为什么有它）；「自定义岗位叫什么」框不预填「社媒运营」。
   第 ① 步：自带接口模板里「阿里云百炼」不重复出三个；品牌档案卡「公司全称」只有一个来源、语言 / 社媒标签用中文、价格带币种、「（这个页面不存在（404））」不套括号；「看着没问题」点了给一句回执。
2. **找红人回话**（`packages/kol-core/src/playbook.ts` 约 371、381 行起）：回话列前 5 个名字 + 「去候选池看全部」链接；不够数时说原因（库里只有 N 个在这个区间）并给两个动作：关联官方数据接口 / 导入一张表；摘要里的工具名 `search_creators` 换人话。候选池没接数据源时那段两百字压成两句 + 两个按钮。「接受这份清单」一条都没新建时说清楚为什么（都已在合作里）并给下一步。
3. **云账号一跳**（`components/onboarding/ai-step.tsx`、`apps/server/src/cloud-account.ts` 约 209 行）：发送中给「正在连 Agents 工坊云…」；失败给「网络不通，再试一次」按钮 + 「先逛演示数据」，不只报网址。
4. **没关联也看得到价**（`components/settings/credits-panel.tsx`）：价目三块、充值四档、增值服务卡照常显示，只把「充值 / 订阅」按钮换成「先关联」。价目数据来源：已关联走云；没关联走本地 `packages/metering/src/pricing.json`（与云同源），标一句「以关联后显示为准」。

## 验收
- 走查第 3、5、6、7、8、9、14、15、17、44 步「通」；证据图到 `docs/assets/wp142/`。
- 新测试：主要目的 → 预勾；第 ④ 步必需 / 可选折叠；找人回话含名字与链接、不够数的两个动作；未关联时价目可见。

## 验证（审核方全量用）
`vitest run apps/workstation apps/server packages/kol-core packages/metering`；动了 playbook 就跑 fast 模拟两个包三个运行时。
