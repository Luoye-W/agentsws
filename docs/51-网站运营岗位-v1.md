# 51 · 网站运营岗位 v1（方案稿）

| | |
|---|---|
| 状态 | Luoye 已拍板 N0–N5（2026-09-15）。**N0 与 N1 的迁移部分由 WP62 实现**，落点见 §1 末与 §2 末；WP63 / WP64 按 §5 排队 |
| 职责拆法（09-15 Luoye 手写） | **网站运营** = 店铺管理（内含店铺设置、商品管理、促销与折扣、数据日报、评价管理）+ 内容与博客 + 邮件营销 + 订单履约 |
| 前置（09-15 Luoye） | 用户设置账户时要**选网站是基于什么搭建的**；目前所有设置只针对 Shopify，WooCommerce / Magento 等待增加 |
| 关联 | 04（旧拆法，将按本文与 Luoye 的后续手写重写）、50（状态板）、44（范围：店铺 / 品牌 / 产品线）、43（Shopify 接入：Admin 写动作→ChangeKind 对照、主题走 CLI、Dev MCP）、15（变更账本与 guardrail）、14（审批项）、29 / 36（面板积木）、46（首次设置） |

## 0. 一句话

**先在账户设置里选平台，再决定这个岗位能连什么、能做什么。** 网站运营四条职责的"读"全走平台只读动作，"写"全走 staged change → guardrail → 审批 → apply（15 的老规矩）；额度按**动作**分，不按子领域分。Shopify 现在就能做（35 条写动作已在对照表里），WooCommerce 是下一个（连接器里已有 provider），Magento 没有 provider、先灰显。

## 1. 前置：网站平台选择（N0）

| 项 | 做法 |
|---|---|
| 存哪 | 公司档案（46）加 `storefront_platform: 'shopify' \| 'woocommerce' \| 'magento' \| 'other'`，首次设置第 ① 步就问（和"你卖的是"并排），设置页可改 |
| 现在支持 | **只有 Shopify**。其它选项灰显并标"待增加"；选了"other"= 没有店铺连接，网站运营岗位只剩内容与博客里不依赖平台的部分 |
| 它影响什么 | ① 连接页只显示该平台的店铺卡；② 职责模板里的 `connectors[]` 按平台解析（`shopify_admin` / `woocommerce` / …），动作对照表（43 §2）按平台一份；③ 岗位面板的"店铺后台"分块按平台取数；④ 登记表（47）里对象的真源标注按平台 |
| 下一个平台 | WooCommerce：OpenConnector 已有 provider，做一份动作对照表 + 一套读动作映射即可；Magento：连接器没有 provider，要先写 provider，排在后面 |
| 不做 | 一个工作区多平台并存（一家公司同时 Shopify + WooCommerce）——用两个工作区或等 44 的范围模型加"平台"一维时再议 |

### WP62 实现落点

| 这一条 | 落在哪 |
|---|---|
| 平台常量表（唯一真源） | `packages/contracts/src/identity.ts`：`STOREFRONT_PLATFORMS`（id / 中文名 / `supported` / `connector_service`）+ `storefrontConnectorService`（这个平台走哪个 provider）/ `storefrontUsableService`（**今天点得动**的是哪一个）/ `storefrontUnsupportedNote`（接不上时那一句人话）。谁都不许再写第二份 |
| 存哪 | `WorkspaceProfile.storefront_platform?`（缺省与非法值一律 `shopify`——存量工作区行为一个字节不变）；事件 `workspace.profile_set` 带上它 |
| 首次设置与设置页 | `apps/workstation/src/components/onboarding/profile-form.tsx`：「你卖的是」与「网站是用什么搭的」**并排**（窄屏一列、宽屏两列）；四个全画出来，只有 Shopify 点得动，其余灰显 + "待增加" tooltip。设置页多一个 `allowUnsupported`——改得动，但点下去先问一次（已连的店铺后台会失效） |
| ① 连接页按平台 | `apps/server/src/connections.ts` 的 `providers()` 按档案过滤店铺卡；邮箱 / GA4 / GSC / 广告与平台无关，一张不少 |
| ② 职责连接器按平台 | 职责 yml 写 `kind: shop`（平台中立），`apps/server/src/onboarding.ts` 的 `providersOf` 按档案解析；平台没有连接器 = 清单里干脆不出那张卡 |
| ③ 面板与工具按平台 | `apps/server/src/{live-data,records}.ts` 的"活跃店铺连接"改为按档案算出的 service（别名 `shopify` 一并认）；`packages/deck` 的 `DataSourceStatus.note` / `ViewSection.note` 把"这个平台还没接"端到面板上。刷新原因码分 `no_connection`（你还没连）与 `platform_unsupported`（我们还没做） |
| ④ 登记表按平台 | `scripts/gen-ontology.mjs` 读平台常量表，给真源在网站平台那一侧的对象加 `platforms`（判据是读动作的 provider 前缀，所以活动 / 会话这些真源在别处的对象一格都不多） |
| 回归 | 3 人 pack `platform/unsupported-platform-says-so`：档案选 WooCommerce → 面板、工具、首次设置清单**三处说同一句话**。DSL 加 `dataset.storefront_platform`、`org.platform_check`、`expected.platform_unsupported` |

截图：`docs/assets/workstation/platform-select.png`（首次设置第 ① 步，非 Shopify 灰显）。

**WP62 没做的**：WooCommerce 的动作对照表与读动作映射（`connector_service` 的名字先占着，连接目录里还没有那张卡）；Magento 的 provider；一个工作区多平台。

## 2. 岗位"网站运营"= 四条职责（N1）

岗位模板默认四条全勾，可去勾；范围（44）挂店铺 / 品牌 / 产品线照旧。旧 `dtc.ops`（15 人 pack）→ `dtc.store` 别名迁移；04 §1 旧的十条职责按下表归并。

### 2.1 店铺管理 `dtc.store`

一条职责、五个能力面，**额度按动作分**：

| 能力面 | 读（只读动作） | 写（staged change → 审批） | 额度 / 自动化上限 |
|---|---|---|---|
| 店铺设置 | 店铺信息、政策页、运费 / 税 / 支付设置、导航、主题列表 | 政策页 / 页面改动 `listing_edit`；主题发布 `publish_theme`（43：走官方 CLI，未发布副本预览） | 页面 L2；主题发布 **永远 L1**；结账 / 支付 / 税这类设置 **不给写动作**（人在后台改） |
| 商品管理 | 商品、变体、库存、集合、元字段 | `listing_edit`（文案 / 图 / SEO）、`price_change`、`publish_product` / `unpublish_product`、集合增删商品、库存 `set` / `adjust` | 文案 L2；改价 ≤ `max_price_delta_pct`（默认 20%）L2、超额 L1；上下架 L1；库存调整 ≤ `max_inventory_adjust`（默认 50 件）L2，直接设定 L1 |
| 促销与折扣 | 折扣码、自动折扣、活动列表 | `discount_code`（营销发码，与客服的安抚发码分账）、`promotion`（自动折扣 / 活动） | 单码 ≤ `max_promo_discount_pct`（默认 30%）且限量 L2；全站活动 / 无上限码 L1 |
| 数据日报 | 销售 / 订单 / 转化 / 流量（Shopify + GA4 + GSC） | 无写；出"日报卡"与"异常卡" | L3 |
| 评价管理 | 评价列表、评分（经评价应用连接器：Judge.me / Loox，**待增加**） | 回复评价（草稿）、邀评 | 回复 L2；差评处理转客服 `dtc.support`；邀评 L2 且合规词表 |

- **guardrail**（15）：`target_in_range`（44）、`price_change` 幅度、`publish_*` 永远人审、活动级促销永远人审；Amazon 那套出站硬闸不适用。
- **面板积木**（36）：数字块：总销售额、订单数、转化率、库存告急数；分块：店铺后台（已有四块）、待审改动（listing / 价 / 上下架 / 促销各一条车道）、库存告急表、差评表、日报卡。
- **卡片**：改价 / 上下架 / 促销 / 主题发布的 staged change 卡（14 四段式已有）、日报卡（L3 自动，看完即归档）、异常卡（销售骤降 / 库存断货 / 转化异常）。

### WP63 实现落点（§2.1）

| 这一条 | 落在哪 |
|---|---|
| 五个能力面的动作与额度 | `packages/roles/roles/dtc/store.yml`：**十一条写动作**，额度各挂各的 `mandate.caps`——改价 20%、库存 50 件、单码 30% + 限量 500、集合一次 20 件、评价回复线 3 星、邀评每天 50。结账 / 支付 / 税**没有动作**，而且 `store_config` 那条 scope 只有 `read`：不是"有动作但不给批"，是这条职责压根没有 stage 那个域的权限 |
| 缺的 ChangeKind | `packages/contracts/src/changes.ts` 只加不删：`inventory_adjust`（`after.mode` 分 `adjust` / `set`）、`collection_edit`（从 `listing_edit` 里分出来——改一件商品的文案只影响那一件，改货架影响整家店）、`review_reply` / `review_invite`。`ObjectType` 补 collection / page / article / inventory_item / review（前四个在 `ShopifyTargetType` 里早就在用，只是一直靠 `(string & {})` 混进来，47 J1 的登记表登记不上） |
| 写动作对照表 | `packages/connect-adapter/src/shopify-actions.ts`：库存两条口归 `inventory_adjust`、集合四条归 `collection_edit`、文章两条归 `publish_post`（草稿与发布的分档在 `after.published`）。评价那一节**故意是空的**：Shopify 原生没有评价，两条 kind 等评价应用的连接器 |
| 副作用表 | `packages/connect-adapter/action-side-effects.yml` 补店铺设置那一面的**只读**动作（政策页 / 导航 / 运费 / 支付 / 税 / 销售汇总）。写侧一条对应的都没有——N2 那句话就是这样落地的 |
| guardrail | `packages/core/src/guardrail.ts`：`HARD_L1` 加 `publish_product` / `unpublish_product` / `promotion`（永远人审，且不是职责 yml 里那行可被策略放宽的 `ceiling`）；`TARGET_SCOPED_KINDS` 加四条新 kind（44 的范围门原样复用）；`RECORD_READ_KINDS` 一张表驱动"改前必读"（文案 / 集合 / 评价回复，判据是 provenance 这个**事实**，不是调用方自报的一格）；`REVIEW_INVITE_FORBIDDEN` 合规词表命中 **block** 而不是 review |
| 库存的两种写法 | `mode: adjust` 看 `max_inventory_adjust`；`mode: set` **不看数字**一律人审——把一个仓写成 0 和写成 1000 在 API 上是同一次调用。fail-closed 两条：不知道现在有多少 → block（不当 0 算）、加减之后是负数 → block |
| 营销发码与安抚发码分账 | 同一条 `discount_code`，两组 cap：客服那条职责配 `max_presales_discount_pct`，店铺管理配 `max_promo_discount_pct` + `max_promo_uses`。配了哪组判哪组，没配的一条不多判；**无上限码**（没有 `usage_limit`）永远人审 |
| 面板积木 | `packages/deck`：四条车道（`PENDING_LANES`，加一条只要加一行）、库存告急表 + 「库存告急数」数字块、差评表、日报卡（五行数全部从结构化行算出来）。异常卡在 `packages/deck/src/anomalies.ts`——只产结论不产卡，**算不出就不出卡**（GA4 没连 → 一张转化异常卡都不出，不拿 0 去比出个 -100%） |
| 阈值 | `RoleDefinition.thresholds`（契约只加一个可选字段）：`low_stock_quantity` / `sales_drop_pct` / `conversion_drop_pct` / `bad_review_rating`。与 `mandate.caps` 的分工：caps 越了就**拦**（权限），阈值过了只**出一张卡**（提醒） |
| 评价应用"待增加" | `apps/server/src/catalog.ts` 的 `PLANNED_CONNECTORS`（Judge.me / Loox）：出现在连接页上、灰着、点不动、没有表单，但说得出"我们还没做"。面板上那一块的 `note` 也照实说——`ViewSection.note` 有值就**不给「去连接」按钮**（点进去无处可点的按钮会让人以为是自己填错了） |
| 模拟 | 3 人 pack `store/price-change-over-cap`（额内 allow / 超额转人审两半都验）、`store/publish-product-needs-approval`（报 L3 也被 `hard_ceiling` 拉回，`auto_approved: 0` 是反证）；15 人 pack `store/daily-report-card`（`daily_reports: 1` + `staged_changes: 0`） |

截图：`docs/assets/workstation/store-position.png`（网站运营岗位面板：库存告急 + 日报卡 + 待审改动四条车道 + 评价应用「还没连」那句话）。

**一条要记在案上的收窄**：51 §2.1 那张表里的 L2 是**职责给的上限**；平台层另有一条更紧的规矩（31 §3.4）——v1 只有 `low` 风险的变更可以超过 L1。于是改价（medium）额内也仍然落在人的队列里，只有集合 / 评价 / 单码这些 low 的才真的自动批。要让改价自动，得先在 15 §2 里把它的风险级改掉，那是另一场讨论。测试 `packages/txn/test/store-guardrail.test.ts` 末尾把这一条写成了断言。

> **WP62 迁移部分已实现**：`dtc.ops` → `dtc.store` 的别名（`packages/roles/src/load.ts` 的 `ROLE_ID_ALIASES`，只可加行）；职责定义从 15 人 pack 自带搬成内置 `packages/roles/roles/dtc/store.yml`（scopes、五个写动作、额度、面板那一块**一个数没改**，连接器改成 `kind: shop`）；15 人 pack 的 roles / assignments / scenarios 改到新 id；首次设置多一个岗位模板「网站运营」= `dtc.store` 一条（`packages/roles/positions/web-ops.yml` 与 `apps/server/src/org.ts` 的 `SEED_POSITIONS`）。上表那五个能力面的**完整**能力（店铺设置 / 评价管理这两面现在还没有动作与面板）由 WP63 补。

### 2.2 内容与博客 `dtc.content`

| 项 | 内容 |
|---|---|
| 干什么 | 博客文章、独立页面（FAQ / 关于 / 活动页）、页面 SEO 元字段；图片素材向设计岗（待做）下需求单 |
| 读 | 文章 / 页面列表与正文、SEO 字段、知识库（19：内容可以引用事实卡，但不能编数字） |
| 写 | `publish_post`（新建 / 更新草稿 L2；**发布 L1**）、页面 `listing_edit`（L2）、SEO 元字段 `set_metafields`（L2） |
| 额度 | 每日发布上限 `max_posts_per_day`（默认 2）；发布永远人审 |
| 面板 | 草稿队列、待发布、近 30 天发布与流量（GSC） |
| 现有底子 | Shopify `create_page / update_page / create_article / update_article / set_metafields` 已在写动作对照表；`publish_post` 已是 ChangeKind |

### WP63 实现落点（§2.2）

| 这一条 | 落在哪 |
|---|---|
| 职责定义 | `packages/roles/roles/dtc/content.yml`：两条写动作——`stage_publish_post`（caps `max_posts_per_day: 2`）与 `stage_listing_edit`（页面与 SEO 元字段）。元字段**没有**自己一条动作：动作 id 与 15 §2 的 kind 是一一对应的，硬给它一个 `stage_set_metafields` 得到的是一条没有 kind 的动作——没有 kind 就没有风险级、没有额度、没有执行器 |
| 草稿 L2 / 发布 L1 | 分档在 guardrail 的 `after.published`，**不在** `HARD_L1`（进了那张表连草稿都自动不了）。`max_posts_per_day` 只算发布，不算草稿——攒十篇草稿不伤人，一天推十篇到店里会把订阅的人推走 |
| 19 不能编数字 | `knowledge` 域只给 `read`；grounding 规则 `facts_before_writing` 把「查事实卡」摆成写文之前的必经一步 |
| 面板 | 草稿队列、待发布（`changes.pending_publish_post` 车道）、近 30 天发布与流量。**GSC 没连 → 流量那一列是「—」不是 0**：0 会被读成"这篇没人看"，而事实是"我们不知道" |
| 岗位模板 | `packages/roles/positions/web-ops.yml` 与 `apps/server/src/org.ts` 的 `SEED_POSITIONS` 都加 `dtc.content`；`apps/server/src/server.ts` 的 `BUNDLED_ROLES` 装它 |
| 模拟 | 3 人 pack `content/blog-draft-then-publish`：草稿 allow、发布 `publish_post_needs_review`。中间隔三小时不是凑数——同一篇文章的两次提案共用去重键，后一条会把前一条顶掉（14 §5） |

### 2.3 邮件营销 `dtc.email-marketing`

| 项 | 内容 |
|---|---|
| 干什么 | 弃购挽回、复购 / 再营销、新品与活动邮件、分群 |
| 连接器 | **待增加**：Klaviyo（首选，独立站主流）、Shopify Email；先做 Klaviyo |
| 读 | 分群、模板、活动效果（打开 / 点击 / 转化）、订阅状态 |
| 写 | `campaign_send`（**发送永远 L1**）、分群规则（L2）、自动流（弃购 / 复购）的开关与文案（L2，且流的触发条件不可由 Agent 放宽） |
| 额度 | 每周发送次数 `max_campaigns_per_week`（默认 2）；退订 / 抑制名单必查（与 WP55 建联那套同一份规则） |
| 面板 | 待审发送、自动流状态、近 30 天效果 |
| 现有底子 | `campaign_send` 已是 ChangeKind；抑制名单逻辑在 `packages/channels` |

### WP64 实现落点

| 这一条 | 落在哪 |
|---|---|
| 职责定义 | `packages/roles/roles/dtc/email-marketing.yml`：三个写动作（`stage_campaign_send` / `stage_segment_edit` / `stage_flow_edit`），额度挂在动作上（`max_campaigns_per_week: 2`）。连接器写 `kind: email_marketing`，**不写死 Klaviyo** |
| 发送永远 L1 | `packages/contracts/src/changes.ts` 的 `ChangeKind` + `@agentsws/core` 的 `HARD_L1` 里都有 `campaign_send`；职责 yml 上再写一次 `hard_ceiling: true`（两处都说，改一处忘另一处时有人喊） |
| 抑制 / 退订名单 | **全仓库只有一份规则**：`packages/core/src/suppression.ts`。WP55 立在 Amazon 出站硬闸里的那条「名单上的人收不到主动外发、在他自己的线程里回信放行」抽了出来，客服（`support-core/amazon/outbound-guard.ts`）与营销调同一个函数。营销这一侧 **fail-closed**：提案不报 `suppression_checked` 就 block（"没问过"与"问过了没人"必须分得开） |
| 触发条件不可放宽 | `PROTECTED_FIELDS.flow_edit`（trigger / trigger_conditions / …）+ guardrail 里 `flow_trigger_not_loosened`（延迟改短、上限调大一律 block）。两条都是 block 不是 review：卡面上看不出一条流会多发给几万人 |
| 连接器 | `apps/server/src/catalog.ts` 加 `klaviyo`（原生表单 + 只读动作）与 `shopify_email`（占位），`CatalogEntry.planned` 一出现就 `available: false`，`begin` / `submit` 当场 503。`connect-adapter/action-side-effects.yml` 补分群 / 模板 / 自动流 / 活动效果 / 抑制名单的读动作 |
| 面板与卡 | `packages/deck`：数据源 `email_marketing`，查询 `email.pending_sends`（走审批库）/ `email.flows` / `email.campaign_performance`（没连 → 「去连接」）；卡上出「发给多少人 / 剔掉了几个」，**剔了 0 个也报**（`DeckHighlight` 的 `audience` / `suppressed`） |
| 回归 | 3 人 pack `email/campaign-send-always-l1`：提案报 L3 → 硬顶按回人审 → 没人点 → 一封没发；Anna 退订的那一个被剔掉并写进卡面。DSL 加 `email.unsubscribe` / `email.campaign_send` 与 `expected.campaign_send` |

**WP64 没做的**：Klaviyo 的真调用（分群 / 模板 / 活动效果现在读不到真数据）、Shopify Email、`segment_edit` / `flow_edit` 的施行口（连接器不在，批了也没地方施行——模拟执行器照实说这句话，不是一句"未实现 kind"）。

### 2.4 订单履约 `dtc.fulfillment`

| 项 | 内容 |
|---|---|
| 干什么 | 待发货处理、拆单、物流单号回填、延误 / 异常主动通知、3PL / 海外仓对接 |
| 连接器 | Shopify 订单与履约（已通）；物流追踪（17track / AfterShip，待增加）；3PL（待增加，按用户需求） |
| 读 | 未履约订单、履约状态、物流轨迹 |
| 写 | `create_fulfillment`（标记发货 + 单号）L2；拆单 L2；取消 `cancel_order` **L1**；延误通知草稿走客服的 `outbound_draft`（L2 → L3） |
| 额度 | 单日自动标记发货上限 `max_fulfill_per_day`（默认 50）；超期未发（> 3 天）出异常卡 |
| 面板 | 待发货、超期未发（已有）、物流异常、今日发货数 |
| 现有底子 | `create_fulfillment / cancel_order / update_order` 在写动作对照表；`orders.overdue` 积木已有 |

### WP64 实现落点

| 这一条 | 落在哪 |
|---|---|
| 职责定义 | `packages/roles/roles/dtc/fulfillment.yml`：四个动作（`stage_create_fulfillment` / `stage_split_order` / `stage_cancel_order` / `notify_delay`）。超期阈值 `overdue_days: 3` 写在 caps 里，**不写死在查询里**——不同类目的发货节奏差得远 |
| 单号是硬的 | guardrail 的 `create_fulfillment`：没有承运商或单号一律 block（不是提醒）。没单号的"已发货"比"未发货"更糟——顾客查不到轨迹、客服无从回答，而订单状态已经变了。已发货的不许再发一次、也取消不了 |
| 取消订单永远 L1 | `@agentsws/core` 的 `HARD_L1` 里加了 `cancel_order`；路由到 owner（退款与库存一起动，不是范围管理者该替公司拍的板） |
| Shopify 写口 | `connect-adapter/src/shopify-actions.ts`：`create_fulfillment` 与 `cancel_order` 从"说不出口"挪到有 kind（`cancel_order` 那条 not_stageable 当初写的就是"要做得先定 `cancel_order`（不可逆、多半 L1 永远）"）。补发与拆单反查回来是同一个动作（`also_change_kinds`） |
| 连接器 | `aftership`（原生表单 + 只读轨迹）与 `track17`（占位），状态同为"还没接"；`action-side-effects.yml` 补几个不叫 `list_` 的读动作 |
| 面板与卡 | `packages/deck`：`orders.unfulfilled`（待发货）、`orders.overdue`（**复用 WP20 那条**，同一个判据不写第二遍）、`fulfillments.today`（按**施行时间**从账本上数，不数订单的存量状态）、`shipments.exceptions`（没连 → 「去连接」）；卡上出承运商 + 单号与"压了几天" |
| 回归 | 3 人 pack `fulfillment/overdue-alert-and-fulfill`：巡检真读 `list_orders` → 出事件 + 通知 → 标记发货 L2 自动施行。DSL 加 `fulfillment.sweep` / `fulfillment.mark_shipped` 与 `expected.overdue_orders` |

截图：`docs/assets/workstation/fulfillment-lane.png`（订单履约分块：待发货 + 超期异常）。

**WP64 没做的**：物流轨迹的真调用（AfterShip / 17track 都还是骨架，"物流异常"那一块照实说没连）、3PL / 海外仓、`update_order` 的通用写口（按字段拆成具体 kind 之前不给）。

## 3. 不做什么（N2）

- 不给结账 / 支付 / 税 / 域名这类"改一下就影响收款"的设置写动作，人在平台后台改。
- 不做一个工作区多平台。
- 评价、邮件营销、物流、3PL 的连接器按"待增加"排队，本期只把职责、动作、额度、面板骨架立起来；连接器不在，面板明说"还没连"（36 §3 的老规矩）。

## 4. 模拟（N3）

3 人 pack 加三条、15 人 pack 加两条，三运行时：
- `store/price-change-over-cap`：运营 Agent 改价超 20% → L1 人审 → 批准 → apply 经 mock connect（15 人 pack 已有改价场景，补"超额"分支）
- `store/publish-product-needs-approval`：上架永远人审
- `content/blog-draft-then-publish`：草稿 L2 自动、发布 L1
- `fulfillment/overdue-alert-and-fulfill`：超期未发出异常卡 → 标记发货 L2
- `store/daily-report-card`：日报卡 L3 自动出、看完归档

## 5. 派工（N4）

| WP | 做什么 |
|---|---|
| WP62 平台前置 ✅ | `storefront_platform` 进公司档案与首次设置；连接目录 / 职责连接器 / 动作对照 / 面板取数按平台解析；Shopify 之外灰显"待增加"；`dtc.ops` → `dtc.store` 别名迁移（**2026-09-15 完成待审**，落点见 §1 末与 §2.1 末） |
| WP63 店铺管理 + 内容与博客 ✅ | 两条职责模板（scopes / 写动作 / 额度 / 自动化上限）、guardrail 补幅度与上限、面板积木与卡片、三条模拟（**2026-09-15 完成待审**，落点见 §2.1 末与 §2.2 末） |
| WP64 邮件营销 + 订单履约 | 两条职责模板、Klaviyo / 物流追踪连接器骨架（目录 + 只读动作，凭据原生表单）、面板与卡片、两条模拟 |
| WP63 店铺管理 + 内容与博客 | 两条职责模板（scopes / 写动作 / 额度 / 自动化上限）、guardrail 补幅度与上限、面板积木与卡片、三条模拟 |
| WP64 邮件营销 + 订单履约 ✅ | 两条职责模板、Klaviyo / 物流追踪连接器骨架（目录 + 只读动作，凭据原生表单）、面板与卡片、两条模拟（**2026-09-15 完成待审**，落点见 §2.3 末与 §2.4 末） |

每个 WP 跟真账号验收（你的 glass-bowl 店）。

## 6. 拍板结论（2026-09-15 Luoye 全过）

- N0 首次设置选网站平台，现在只开 Shopify，WooCommerce 下一个，Magento 灰显
- N1 网站运营 = 店铺管理（五个能力面、额度按动作分）+ 内容与博客 + 邮件营销 + 订单履约；旧 `dtc.ops` 迁移为 `dtc.store`
- N2 不给结账 / 支付 / 税写动作；不做一个工作区多平台；缺的连接器排队待增加
- N3 五条模拟场景
- N4 三个 WP 的切法（62 前置 → 63 / 64 并行）
- N5 各项额度默认值（改价 20%、库存调整 50 件、促销 30%、每日发文 2、每周邮件 2、每日发货 50）
