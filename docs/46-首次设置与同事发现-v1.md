# 46 · 首次设置与同事发现 v1

| | |
|---|---|
| 状态 | **已定案，WP51 实现**（2026-09-11）。I1–I6 全部落地，实现落点见 §6；云目录仍按 I2 不做 |
| 起因 | 用户第一次打开要设置公司全称（默认可共享，同公司的同事之间能连上）、勾岗位 / 职责（勾岗位 = 该岗位的职责全勾，可多岗位、可只勾职责），然后把这些岗位职责需要的连接、技能一次配好 |
| 关联 | 20（身份 / 工作区 / Join）、45（个人 → 公司合并）、27（岗位模板与职责）、04（职责清单）、36（UI 原则）、40 §1（数据归属） |

## 0. 一句话

第一次打开只问三件事：**你们公司叫什么、你是谁、你做什么**。答完，工具已经知道你要连哪些平台、装哪些技能，连接页和技能页变成一份待办清单；同一家公司的同事装了同一个工具，可以互相看见并申请连上，连上之后走 45 的合并。

## 1. 首次设置向导（四步，随时可退出，设置页能回来改）

| 步 | 问什么 | 默认 / 规则 |
|---|---|---|
| ① 公司 | **公司全称**（提示"写营业执照上的全称，别写简称"）；公司邮箱域名（可选，从你的登录邮箱自动带出）；**"让用同一个工具的同事找到我"** 开关 | 开关默认**开**。关了 = 独立使用，不参与发现也不被发现，随时可改 |
| ② 你 | 名字、登录邮箱（已登录的直接带出） | — |
| ③ 岗位与职责 | 岗位列表（27 的岗位模板：店主 / 负责人、独立站运营、独立站售后客服、亚马逊运营、投放、社媒 / 红人、设计、建站……）；勾岗位 = 它包含的职责全勾上；可多岗位；可展开只勾职责；每条职责旁有一句"它会干什么" | 至少勾一条职责才能下一步；一个人用时自动加 owner |
| ④ 要配的东西 | 按勾选算出：**要连的平台**（职责的 `connectors[]` 去重：Shopify / 邮箱 / GA4 / Search Console / Meta / 模型）、**要装的技能包**（职责的 skills 与 app 依赖）；每项一个"去连 / 去装"按钮，也可以"先跳过" | 已连的打勾；这一步做完 = 首页不再出黄条"还没接模型" |

- 向导只出现在**这个工作区还没设过公司名**的时候；后续从"公司"页和"设置"页都能改。
- 每一步的解释性文字按 36 §7 进 tooltip；不出现任何内部 id。

## 2. 同事发现（I1–I4）

### I1 "同一家公司"怎么认

- 公司名归一化（去空格、大小写、全半角、"有限公司 / Co., Ltd." 这类尾缀）+ **公司邮箱域名**（有就用）。两个都对上才算强匹配；只有名字对上算弱匹配（要人确认）。
- 名字相同不等于同一家：发现只是"看见"，**连上必须有一方申请、另一方 owner 批准**（membership 审批卡，14）。任何时候不因为名字相同就自动共享数据。

### I2 三条发现渠道，v1 做两条

| 渠道 | 怎么工作 | v1 |
|---|---|---|
| 邀请链接 / 邀请码 | 已有工作区的 owner 生成，同事在向导①贴进来 → 直接走申请加入 | **做**（最可靠，永远保留） |
| 局域网发现 | 桌面壳 / 服务进程在本机网段广播"agentsws · <公司名归一化哈希>"（mDNS），同网段同哈希互相看见；只广播哈希不广播全称 | **做**（办公室里最顺手；不经任何服务器） |
| 云目录 | 开关打开时把 `hash(公司名归一化 + 域名)` 登记到 agentsws 托管目录，异地同事能看见 | **留接口不做**（要我们运营一个服务，等托管档一起上；开源版默认没有） |

- 只交换哈希与一个"申请加入"的入口；全称、成员名单、任何业务数据都不在发现阶段出去。
- 开关关着 = 不广播、不查询、不登记；打开的那一刻记事件 `discovery.enabled`。

### I3 看见之后

- 向导①或"公司"页显示"发现 2 位同事也在用（王岚的工作区 · 3 人）"，按钮"申请加入他们"或"邀请他们加入我"。
- 申请 → 对方 owner 收一张 membership 卡（同意 / 拒绝）；同意后走 20 §4 Join + 45 的合并向导（品牌 / 产品线 / 店铺范围对照、凭据要本人交出）。
- 两个都是一个人的工作区互相申请：先申请的一方并入后申请的一方？不对——**谁 owner 批谁是目标**；两边都批就以先批的为准，另一边的申请自动失效并告知。

### I4 独立使用

- 开关关了就是 20 §1 的个人工作区，什么都不变；以后想加入公司，"公司"页有"加入一家公司"入口（邀请码 / 局域网 / 云目录）。

## 3. 岗位与职责怎么落到配置（I5–I6）

### I5 勾选 = 生成一份"要配的东西"清单，不是一堆开关

- 职责模板（05）里已经写着它要的 `connectors[]`（kind + required）、`skills`、`app` 依赖；向导④只是把勾选的职责这些字段**去重汇总**成清单。清单条目点进去就是连接页的对应卡 / 技能页的对应包，不另做一套配置界面。
- 权限（"岗位职责对应需要的权利接口"）= 05 的 scopes，随职责模板自带；用户不用手配权限，配的是**连接**（没连上 = 岗位 `ready: false`，面板明说，WP47 已做）。

### I6 一个人勾了多个岗位

- 一个人用：每勾一个岗位就建一个 Assignment（人 × 岗位 × 范围），范围先挂向导④连上的店（连上 Shopify 就自动挂那家店），没店就挂空并在面板明说。
- 只勾职责不勾岗位：建一个"自定义岗位"（名字让用户填，默认"我的岗位"），职责就是勾的那几条；27 的岗位模板不变。

## 4. 不做什么

- 不做公司名唯一性校验（名字不是身份，批准才是）。
- 不在发现阶段传任何业务数据；不做"自动合并"。
- 云目录 v1 不做；开源版没有任何默认外发。

## 5. 改动落点（当初的估）

| 层 | 改什么 | 量 |
|---|---|---|
| 契约 | `Workspace.profile { legal_name, domain?, discoverable }`；`Position`/`Assignment` 的自定义岗位；事件 `workspace.profile_set`、`discovery.enabled/disabled`、`discovery.peer_seen`、`membership.requested/approved/rejected` | 小 |
| 服务进程 | 首次判定（没设过公司名 → 向导）；`discovery.ts`（mDNS 广播 / 监听，只带哈希；邀请码签发 / 校验）；membership 审批卡 | 中 |
| API | `/v1/workspace/profile`、`/v1/onboarding/plan`（勾选 → 清单）、`/v1/discovery/peers`、`/v1/invites`、`/v1/memberships/requests` | 中 |
| 工作台 | `pages/onboarding.tsx` 四步；公司页"加入 / 邀请"区；设置页可改公司档案与开关 | 中 |
| 模拟 | 3 人 pack 加一条：两个人各自首次设置、公司名相同、局域网发现（替身）→ 申请 → owner 批 → 走 45 合并 | 小 |

---

## 6. 实现落点（WP51 实际做出来的）

### 6.1 代码

| 层 | 文件 | 做了什么 |
|---|---|---|
| 契约 | `packages/contracts/src/identity.ts` | `WorkspaceProfile`、`Invite`、`MembershipRequest`（只加不删） |
| 契约 | `packages/contracts/src/events.ts`、`approval.ts` | 九条事件 + `ApprovalKind` 加 `membership` |
| 纯函数 | `packages/core/src/company.ts` | `normalizeCompanyName` / `normalizeDomain` / `companyKey`。**住在 core 而不是服务进程里**，因为模拟回路也要拿它算钥匙——两边各写一份的话那条模拟题就只是在测它自己 |
| 服务进程 | `apps/server/src/onboarding.ts` | 首次判定、公司档案存取、勾选 → 清单（`expandRoles` / `planOf`）、`apply` 真建分配 |
| 服务进程 | `apps/server/src/discovery.ts` | mDNS 广播与监听（`bonjour-service`，MIT）；TXT 只有 `k=<company_key>` 与 `v=1`；起不来降级 |
| 服务进程 | `apps/server/src/invites.ts` | 邀请码签发 / 核销、申请加入、`membership` 审批卡、批准后建成员、I3 的互相失效 |
| API | `packages/api/src/routes/onboarding.ts` | 12 条路由（见 20 §5 那张表） |
| 工作台 | `apps/workstation/src/pages/onboarding.tsx` + `components/onboarding/*` | 四步向导、加入 / 邀请区、公司档案表单 |
| 工作台 | `pages/org.tsx`、`pages/settings.tsx` | 公司页"加入一家公司"页签；设置页改公司档案与开关 |
| 模拟 | `packages/simulation/src/world.ts`、`scenario/*`、`packs/dtc-15p/scenarios/org/two-solo-users-discover.yml` | `org.first_run` / `org.join_request` 两条 DSL 事件；15 人 pack 12 → 13 条 |

### 6.2 归一化到底剥什么（§2 I1 的那一句话展开）

按顺序：① 全角 → 半角（含 U+3000 表意空格）→ ② 全部小写 → ③ 分隔性标点（`, . 、 ·`）变空格、
折叠空白 → ④ 反复剥公司形式尾缀，剥到不动为止 → ⑤ 剩下的空白与括号 / 连字符 / 引号一律去掉。

- **中文尾缀按 `endsWith` 剥**（中文本来不分词）：股份有限责任公司 / 股份有限公司 / 有限责任公司 /
  责任有限公司 / 有限公司 / 股份公司 / 有限会社 / 合同会社 / 株式会社 / 公司。
- **拉丁尾缀按词剥**，不是按后缀剥——按后缀剥会把 `Visa` 剥成 `Vi`（尾缀表里有 `sa`）：
  company limited / co limited / co ltd / pte ltd / pty ltd / incorporated / corporation /
  company / limited / corp / inc / ltd / llc / llp / plc / gmbh / sarl / sas / spa / srl /
  pte / pty / nv / bv / ag / sa / kk / co。
- **只剥公司形式，不剥行业词**：「科技」「实业」「贸易」留着，它们是名字的一部分
  （"诺伏特科技" ≠ "诺伏特"）。
- **剥空了就不剥**：名字本身就叫「公司」的那种，剥到只剩空串时停手。

`company_key = sha256(归一化名 + '|' + 域名小写)`。域名归一化去协议、去 `@` 前缀、去 `www.`、
去尾路径。没填域名时后半截是空串——算出来仍是一把合法的钥匙，只是它只代表弱匹配；
**这不改变任何事**，因为看见从来不等于连上。

### 6.3 与规范不同 / 规范没说的地方

1. **邀请码明文存本机库**（WP28 的邮件邀请 token 只存 sha256）。理由：owner 关掉页面之后
   还得把码念给同事听；而它本身不是凭据——拿着它最多能让对方队列里多一张卡。
   事件日志里仍然只有指纹。
2. **过期码与用完的码一律 404 且不区分**，免得拿它探测哪些码存在过。
3. **`needs_setup` 多看一条**：除公司档案没设过之外，还要求"除所有者外没有任何分配"。
   §1 末段只说了前一条，但一个已经用了半年、当初跳过了向导的工作区不该在第 100 天被弹一次。
4. **"先跳过"只活在这一个标签页的内存里**。40 §1.2 那条边界（个人电脑上不存真源）在工作台里
   是拿一张白名单钉死的，"这一次先不弄"不属于偏好 / 会话凭据 / 未发送草稿三类，所以不落盘。
5. **20 §4 Join 的 import 入口这个仓库里还没有**（WP50 在做）。所以"同意 → 交给 Join"这一步
   现在是：建人 + 建成员 + 一条 `common.member` 职责，发 `membership.approved` 且 payload 带
   `next: 'join_import'`。入口落地后在 `invites.ts` 的 `decide` 里换成一次调用即可。
6. **云目录**按 §2 I2 不做，连接口都没留——`MembershipRequestVia` 里有 `'directory'` 这个值，
   但没有任何一条路径会产生它。

### 6.4 截图

- `docs/assets/workstation/onboarding-step1.png`：向导第 ① 步（公司档案 + 发现开关 + 加入一家公司）
- `docs/assets/workstation/onboarding-step3.png`：向导第 ③ 步（勾岗位 = 职责全勾，展开可只勾职责）
- `docs/assets/workstation/onboarding-step4.png`：向导第 ④ 步（要配的东西清单，模型没接排第一条）
