# 品牌资产（副本）

这里是母品牌**「出海Agents工坊」**标记的 SVG 副本，给仓库门面（`README.md`）与文档用。

| 文件 | 用在哪 |
|---|---|
| `mark-dark.svg` | 深底上（GitHub 深色主题、深色封面） |
| `mark-light.svg` | 浅底上（压暗端点——亮端点在纸白上黄那一头几乎消失） |
| `mark-mono.svg` | 单色，`currentColor`；印刷、灰度、以及任何小于 28px 的地方 |

## 真源不在这里

真源是母品牌规范 **`00_Brand/品牌设计规范-v2.md`**（几何 §1.1、整体渐变 §1.2、
禁止项 §1.5、动效 §3）。

**几何改动回那边改**，然后：

1. 同步 `packages/brand`（`@agentsws/brand`——规范在代码里的唯一副本，
   `test/geometry.test.ts` 逐格对着规范核，改错了测试会红）；
2. 重跑 `python3 scripts/gen-brand-assets.py`，这三张 SVG、工作台的 favicon、
   桌面壳的图标与托盘图都是它一次出的。

**不要手改这三个文件**，也不要在别处再画一版标记。

## 几条不许

- 不改块的相对位置和间距（那是识别的全部）
- 不允许单块自己带渐变 —— 静态的渐变永远铺在整个标记上（动效是另一回事，见规范 §3.0）
- 不加描边、投影、发光；不旋转、不斜切、不做透视
- 浅底不直接用深底那套亮端点
- 不把「出海Agents工坊」六个字塞进标记里

产品里怎么用（哪种动效代表什么语义、哪里禁用）写在 **`docs/36` §12**。
