# 自主决定汇总（待 Luoye 审）

| | |
|---|---|
| 日期 | 2026-09-08 |
| 范围 | 14–29 十六份第一版规范里，我没有问你、按自己判断定下的东西。按"改起来的代价"排序：先看 A 类 |
| 怎么用 | 每条一行；不反对就算定案；反对的写编号和你的意见，我改规范 |
| 09-08 更新 | 独立评审对 A 类 11 同意 / 19 反对，我在 `31` 里逐条接受了其中 A2、A3、A7、A9、A10、A11（改为含在途）、A12（禁止淘汰已 stage 引用）、A14（参数化）、A18、A19（保留差异展示）、A21、A23（声明更新也走升级审批）、A25、A26（不冻结 Inngest）、A27（按风险与有效期）、A29（业务幂等另建）、A30（关键依赖缺失即停）的修正；A15、A16 保留为待数据校准的默认值 |
| 置信度 | 高 = 有上游范本或前文已定原则推出；中 = 合理但有替代方案；低 = 拍脑袋的默认值 |

---

## A 类：影响数据模型或多个模块，晚了难改

| # | 决定 | 在哪 | 置信度 | 替代方案 |
|---|---|---|---|---|
| A1 | 事件日志与所有记录都带 `schema_version`，新版并排、写永远最新、读经迁移器；旧读取器保留一个大版本 | 21 §1 §5 | 高（dsh 同款） | — |
| A2 | 个人数据加密分片，GDPR 删除 = 销毁密钥；事件日志本身永不删 | 21 §4 | 高 | 真删除（破坏 append-only） |
| A3 | 每条业务记录信封固定：workspace / owner / scope / sensitivity / source / version（乐观锁） | 21 §2 | 高 | — |
| A4 | 无状态运行：运行不读上次会话文件，历史经 `summary` / `thread` ContextItem 注入；dsh 会话只作审计轨迹 | 17 §5 | 高（03 已定） | — |
| A5 | 新增两个事件 `context.injected`、`prompt.assembled` 作为铁律的自动校验点 | 17 §2 | 高 | — |
| A6 | 审批项每条有且只有一个 role_id；跨职责的事用两个项互相引用 | 14 §2 | 中 | 允许多职责标签 |
| A7 | 去重键按 kind 定义，待审时重复提交更新原项（revision+1） | 14 §5 | 高（teamai） | 每次新建 |
| A8 | L2 / L3 额度内自动通过**仍建审批项**（auto_approved + 抽检） | 14 §0 | 高 | 不建项只记事件 |
| A9 | Guardrail 两种后果：超上限 = require_review（转人审），状态条件 / 受保护字段 / 集合重复 / 改前未读 = block | 15 §3.2 | 高（Commerce Agents） | 全部 block |
| A10 | apply 时重读记录：不可幂等 kind（退款 / 补发）记录变了即失败；可幂等 kind 继续但记 note | 15 §5 | 中 | 一律失败 |
| A11 | 累计防绕过：同目标 30 天累计上限 = 单次上限 × 1.5 | 15 §4 §10 | 低 | 不做累计 |
| A12 | Provenance 每类上限 200，LRU 淘汰并记事件 | 15 §6 | 中（上游值） | 不设上限 |
| A13 | OpenConnector 目录没有副作用字段，我们维护 `action-side-effects.yml`，未标按 write | 18 §1 | 高 | 全部人工标 |
| A14 | 入站管线：去重 24h 窗口、重试 5 次指数退避、死信进 owner 车道 | 18 §2.2 | 中 | 数值待调 |
| A15 | 事实卡置信度状态机沿 Co-Engram（unverified→…→verified / refuted）；晋升 verified 判据沿 teamai 数值 | 19 §1 §4 | 中 | 简化为两态 |
| A16 | MemoryFact 纪律原样沿 Commerce Agents（key ≤64、value ≤200、cap 8、每轮 ≤3、写过滤） | 19 §1.2 | 高 | — |
| A17 | 知识检索先按身份过滤候选再算相似度（过滤下推），sqlite-vec + FTS5 + simple 分词 | 19 §3、13 §2 | 高 | 先 top-k 再过滤（会漏） |
| A18 | Person 全局唯一按 email 合并；Workspace.kind personal / shared；部门 Join 公司后变 department range、原 owner 变 manager；源工作区 status=joined 只读 | 20 | 高（03 §7.4） | — |
| A19 | Join 时 policy 层知识不带入（连提议都没有）；其余知识全变提议 | 20 §4.2 | 高 | 带入并审 |
| A20 | 模型网关记账粒度 (workspace, assignment, role, run, purpose)；预算三级（运行 / 分配日 / 工作区日月）；急停 `AGENTSWS_MODEL_HALT` | 22 §3 | 高 | — |
| A21 | 数据驻留策略两项：`data_residency: cn|any` 与 `eu_customer_to_cloud_brain: allow|deny` | 22 §2 | 中 | 更细的按客户市场 |
| A22 | 包清单 `provides.tools[].side_effect` 必填；只有 connector / dsh-plugin-wrapper 两类可含代码 | 23 §1 | 高（16） | — |
| A23 | 分发协议采用 teamai 的 report / sync / ack 三端点形状；含代码的命令只提示不执行 | 23 §4 | 高 | 自定义协议 |
| A24 | skill 段 id 用隐藏 ULID 存 sidecar；overlay 按段 replace / append / remove | 24 §1 | 高（06 §5.1） | 行级 diff |
| A25 | 学习回路置信度算法：确认 +0.15、忽略 ×0.7、驳回归零；夜间整理过线 0.6 | 24 §3 | 低 | 待数据调 |
| A26 | 流程引擎契约暴露 run / approval / wait_event / sleep / action / branch / human_task 七种步骤；默认 Inngest | 25 §1 | 中 | 更少步骤类型 |
| A27 | 定时任务错过触发默认 `run_once_now` | 25 §3 | 中 | skip |
| A28 | 工作台动作永不回模型；digest 的 triage 只预填输入框 | 29 §5 | 高（Commerce Agents） | — |
| A29 | API 唯一入口 `/v1`，破坏性变更并排 `/v2`；所有 POST 支持 Idempotency-Key 24h | 28 §2 | 高 | — |
| A30 | 内核模块清单 `modules.yml` 要签名与允许源；不满足 requires 挂起而非失败 | 28 §1 | 高（Cordis 语义） | — |

## B 类：默认值与阈值，随时可改

| # | 决定 | 在哪 | 置信度 |
|---|---|---|---|
| B1 | 审批项过期：7 天；对外草稿 48h；认领 14 天（你已定） | 14 | — |
| B2 | 升级：24 工作小时 → scope_manager，48h → owner；immediate 4h（前文已定） | 14 §7 | — |
| B3 | 队列"预计 X 分钟" = Σ(kind 平均处理时长 × 数量) | 14 §8 | 低 |
| B4 | 各 ChangeKind 默认额度（退款 50、价格 20%、促销 50%、出价 15%、预算 20%、日花费总闸）——全是占位 | 15 §2 | 低 |
| B5 | 基准货币默认 USD；汇率快照来自网关内置 provider | 15 §10 | 中 |
| B6 | 分块 ≤ 800 token；沉默条目 90 天；高召回低认可阈值 0.5 | 19 §4 | 低 |
| B7 | 缓存：时间截到小时；静态前缀断言逐字节稳定 | 22 §2 | 高 |
| B8 | 模拟门禁：指标劣化阈值 5%；三档触发（提交 / 每日 / 每周） | 26 §4 | 中 |
| B9 | 合成公司规模参数：3 人 300 订单为基线 | 26 §2 | 低 |
| B10 | 3 人 starter 的策略层默认：SoD 关、日花费 300、tz Asia/Shanghai；15 人 SoD 开且退款 > 200 / 价格 > 10% 须他人审 | 27 | 低 |
| B11 | 首页 focus 上限默认 6（你已定） | 29 §3 | — |
| B12 | WS 事件流按 ulid 续传；限流按 workspace × kind | 28 §2 | 中 |

## C 类：范围取舍（我决定"先不做"的）

| # | 决定 | 在哪 |
|---|---|---|
| C1 | v1 只有 local 身份 provider 实现，飞书 / 企微 / 钉钉只定契约 | 20 §2 |
| C2 | 投递 v1 只有工作台 + 邮件；IM 卡片 provider 第二阶段 | 18 §3 |
| C3 | 渠道适配器首批四个：email、WhatsApp Business、飞书 bot、Shopify webhook | 18 §2.3 |
| C4 | 流程定义首批五条（红人合作、订单里程碑、邮件序列、Join、应用升级），v1 只实现引擎 + 邮件序列 | 25 §1 |
| C5 | 27 里供应链 / 财务岗位只作 member，不给职责 | 27 §2 |
| C6 | 反向变更只对可逆 kind 提供；退款 / 补发 / campaign 不提供撤销 | 15 §5 |
| C7 | 分析子代理 SQL 只读白名单沿 Commerce Agents，不做通用 BI | 21 §3 |
| C8 | 模拟 real 模型档只用于 eval，不进合并门禁 | 26 §4 |

## D 类：命名（对外可见的词）

| # | 决定 | 在哪 |
|---|---|---|
| D1 | 审批项五个动作：通过 / 编辑后通过 / 驳回 / 转给别人 / 稍后 | 14 §4 |
| D2 | 认领项四个动作：接受 / 转走 / 不是我的 / 不是事 | 14 §3 |
| D3 | 首页四区叫：队列、告警、今日关注、日报 | 29 §3 |
| D4 | 应用类别标签：职责 / 岗位 / 模板 / 技能 / 卡片 / 连接 / 升级（你已定） | 23 |

---

## 下一步

你审完 A 类（30 条）即可开工；B / C / D 边做边改。开工的第一个里程碑按 09 §4：内核 + 契约骨架 + 模拟回路 fast 档跑通一条端到端场景（26 §1 那条"退货窗口内"），全部用替身。
