# 76 · 插件开放接口 v1

| 项 | 值 |
|---|---|
| 日期 | 2026-09-20，WP119b |
| 是什么 | 任何人都可以给 Agents 工坊写采集插件（Chrome / Firefox 扩展、别的工具）的那组**本机 HTTP 接口** |
| 参考实现 | 开源仓库 `apps/extension`（**功能精简**）；官方完整版不开源，内测用户从私有 Release 拿 zip |

## 0. 一句话

插件跑在用户自己的浏览器里，打到**本机**的 Agents 工坊服务（`http://127.0.0.1:<port>`）；用户在工作台生成 6 位配对码，在插件里输入，那个插件从此拿到一把**只绑它**的令牌。装了本机服务才能用；云端只在本机用户登录云账号时才参与（共享公共红人库），**插件自己永远不持有云令牌**。

## 1. 配对（配对时绑定）

1. 用户在工作台「设置 → 连接 → 浏览器插件」点「生成配对码」：`POST /v1/extension/pairings` → `{ code: "482013", expires_at }`。码是 6 位数字（前导零保留）、5 分钟有效、**一次性**；同一工作区再生成一次，上一码当场作废。
2. 插件拿码来换令牌：`POST /v1/extension/pair`，`Origin` 头必须是**扩展自己的 Origin**（`chrome-extension://<id>` / `moz-extension://<uuid>`）。**配对时绑定**：码换令牌的那一刻，插件 id 就与令牌绑死——之后每个请求都要令牌与 Origin 同时对得上，把令牌抄进别的扩展里没用。
3. 换回来的是 `{ token, token_id, workspace_id, scopes, expires_at }`。**明文令牌只在这一跳出现**，本机库里只有它的哈希；令牌 30 天有效，用一次记一次 `last_used_at`。
4. 工作台上列得出每一把令牌（插件自报名字 + 扩展 id + 配对时间 + 最近使用），**逐个可吊销**：`POST /v1/extension/tokens/:id/revoke`。吊销是写 `revoked_at` 不是删行——用户要看得见什么时候撤的。
5. 配对表**落 SQLite**（本机 `extension-pairing.sqlite`）：重启服务不再要用户重新配对（WP119 留尾的那一条）。

**Origin 纪律**：扩展 id 只从 `Origin` 头来，不从请求体里来——请求体是插件说的，`Origin` 是浏览器说的。非扩展 Origin 一律不认；不开通配 CORS 的纪律不变。

## 2. scope

| scope | 允许什么 |
|---|---|
| `kol.observe` | 上报观测（`POST /v1/extension/observations`） |
| `kol.capture` | 预留：经本机服务写联系方式等需用户显式同意的写入 |
| `kol.read` | 读本机红人库里已有红人的脱敏形态 |

scope 由服务端发令牌时定（现在是三个全给），插件**自报不算数**；接口侧每次都查。

## 3. 端点一览

| 方法与路径 | 鉴权 | 干什么 |
|---|---|---|
| `POST /v1/extension/pairings` | bearer（工作台用户） | 生成 6 位配对码 |
| `GET /v1/extension/tokens` | bearer | 本工作区的令牌清单 |
| `POST /v1/extension/tokens/:id/revoke` | bearer | 吊销一把令牌 |
| `POST /v1/extension/pair` | 无（码 + Origin） | 码换令牌（配对时绑定） |
| `GET /v1/extension/hello` | 无 | 版本协商与能力宣告（见 §4） |
| `POST /v1/extension/observations` | 令牌 + Origin，需 `kol.observe` | 上报一批观测 |

WP119c 追加的一批（完整版面板要的，逐条说明见 §10）：`GET setup`、`POST creators`、
`GET/POST creators/{p}/{id}/report · reveal-pricing · contact(+dispute) · seed-signature`、
`POST contacts · content-observations · contents · bio-link-observations`；`hello`
追加了可选 `workbench_url`。错误一律走服务进程统一信封：`{ code, message, trace_id }`，`message` 是给人看的话。

## 4. hello：版本协商

`GET /v1/extension/hello` 不需要令牌。返回：

```json
{
  "workspace_id": "ws_dtc3c",
  "workspace_name": "NordVolt Gear",
  "cloud_linked": true,
  "shares_to_public_library": true,
  "scopes": ["kol.observe", "kol.capture", "kol.read"],
  "server_version": "0.1.0"
}
```

插件用它做三件事：确认本机服务在不在（哪个端口都是它自己配的）；把工作区名字显示给自己的用户；**如实告知**「登录了就共享到公共红人库」——登录云账号即共享公开数据，没有第二个开关（Luoye 09-19）。`cloud_linked: false` 时采集只进本机。

## 5. 观测的数据格式

`POST /v1/extension/observations`，一次最多一批（建议 ≤ 20 条，离线攒的队列分批补传）：

```json
{
  "observations": [
    {
      "kind": "creator",
      "captured_at": "2026-09-20T03:12:00.000Z",
      "url": "https://www.youtube.com/@somechannel",
      "payload": {
        "channel": "youtube",
        "handle": "somechannel",
        "display_name": "Some Channel",
        "followers": 48213,
        "observed_at": "2026-09-20T03:12:00.000Z"
      }
    }
  ]
}
```

- `kind` 区分观测种类（红人 / 内容 / 联系方式…）；`payload` 里放的是**页面公开可见**的字段，契约 `packages/api` 的 `ExtensionObservation` 有逐字段说明，新字段一律进 `extra`（只加不改）。
- 每条观测按 **URL + 观测时间** 幂等：同一个 URL 重复报是"更新观测"，不是新建两个红人。
- **送出去的是窄行**：登录云账号时，公共红人库只会收到公开页面本来就有的那几格（渠道 / handle / 名字 / 粉丝数 / 类目）——联系方式、备注、内部打分**不出本机**（联系方式走单独的 reveal / 贡献计费，见 `docs/68`）。
- 应用没开时插件把观测攒在本地队列，服务回来再补传；补传按同样的幂等键，重复报不会写重。

## 6. 限流与错误码

- 令牌请求按令牌限流（突发 30 / 每秒 5）；超了回 `rate_limited`，插件应该退避重试。
- 常见错误码的人话：`bad_origin`（这不是扩展发来的请求）/ `bad_code`（码得是 6 位数字）/ `expired`（码过期了，重新生成一个）/ `used`（这个码已经用过了）/ `no_pairing`（没这个码，去工作台重新生成）/ `forbidden`（这把令牌没有需要的 scope）/ `rate_limited`（太快了，歇一下再试）。

## 7. 隐私规则

- 采集只针对**用户自己打开的页面**；插件不做后台爬取。
- 只进本机；上云只在上面的窄行范围内。用户没登录云账号，什么都不会离开这台机器。
- 本机服务不把观测转发给任何第三方；模型分析（打分 / 体检）走用户自己配的模型。

## 8. 30 行最小示例（配对 + 上报一条观测）

```js
// 1) 用户在工作台拿到码后，插件里输入它：
const pair = await fetch('http://127.0.0.1:4317/v1/extension/pair', {
  method: 'POST',
  headers: { 'content-type': 'application/json' }, // Origin 由浏览器自动带上
  body: JSON.stringify({ code: '482013' }),
}).then((r) => r.json());
if (!pair.data) throw new Error(pair.message);
const token = pair.data.token;

// 2) 存好令牌（chrome.storage.local），之后每个请求带上：
const hello = await fetch('http://127.0.0.1:4317/v1/extension/hello').then((r) => r.json());

// 3) 上报一条观测：
const res = await fetch('http://127.0.0.1:4317/v1/extension/observations', {
  method: 'POST',
  headers: { authorization: `Bearer ${token}`, 'content-type': 'application/json' },
  body: JSON.stringify({
    observations: [{
      kind: 'creator',
      captured_at: new Date().toISOString(),
      url: location.href,
      payload: { channel: 'youtube', handle: 'somechannel', observed_at: new Date().toISOString() },
    }],
  }),
}).then((r) => r.json());
console.log(res.data.saved);
```

## 9. 与 WP119 的差别（本单改了什么）

- 配对从"只认预注册的扩展 id"改成**配对时绑定**：任何扩展都能配，绑死在配对那一刻。
- 配对表从内存档升级为 **SQLite 档**（`extension-pairing.sqlite`），语义两档逐条一致，一致性有测试钉住。
- 工作台的令牌清单本来就是按"逐个可吊销"做的，本单没动它的行为。

## 10. WP119c：完整版面板要的那批端点

这一节是 WP119c 加的。清单来自完整版面板的实际需求（私有仓 `docs/parity.md` §六），
全部**只加不改**：`/v1/extension/*` 里已有的形状一个字没动。鉴权沿用 §2 的三个
scope（`kol.observe` / `kol.capture` / `kol.read`），**没有新增 scope**——写库的
用 `kol.capture`，读库的用 `kol.read`，报观测的用 `kol.observe`，每个路由声明自己
要哪个，令牌上没有就是 403（话里说清缺的是哪一个）。

| 方法与路径 | scope | 干什么 |
|---|---|---|
| `GET /v1/extension/setup` | `kol.capture` | 品牌（工作区）/ 活动 / 候选池清单——存入与搜索 FAB 的组织上下文 |
| `POST /v1/extension/creators` | `kol.capture` | 显式存入红人池（upsert 全量快照，写本地红人库） |
| `GET /v1/extension/creators/{platform}/{id}/report` | `kol.read` | 本机观测历史 + 粉丝趋势 + `tenant_pool`（已存状态 / 已有邮箱 / 上次更新）；库里没有 = 404 |
| `GET /v1/extension/reveal-pricing` | `kol.read` | 看一次邮箱的积分价（**不消耗**；价取 `pricing.json` 的 `data.kol.lookup`） |
| `GET /v1/extension/creators/{platform}/{id}/contact` | `kol.capture` | 公共库 reveal（本机代理云端；计费在服务端，余额不足回人话） |
| `POST /v1/extension/creators/{platform}/{id}/contact` | `kol.capture` | 贡献一条联系方式到公共库（云端收；回执说清是新的还是库里已有） |
| `POST /v1/extension/creators/{platform}/{id}/contact/dispute` | `kol.capture` | 标记一条联系方式是错的（免费；云端只记不裁） |
| `POST /v1/extension/contacts` | `kol.capture` | 写进自己红人池的联系方式行（明文进本机加密库，库里只留 key 名） |
| `POST /v1/extension/content-observations` | `kol.observe` | 内容观测。**红线：请求体里没有评论这一格**，多一个键整批拒 |
| `POST /v1/extension/contents` | `kol.capture` | 存入自己的内容库——**唯一**可带已采评论的端点（服务端只保 200 条） |
| `POST /v1/extension/bio-link-observations` | `kol.observe` | 简介外链页（Linktree / Beacons）观测 |
| `GET /v1/extension/seed-signature` | `kol.read` | 种子频道主题词（库里没有这个人 = 404，面板不做预筛） |

逐条的人话：

- **找人的键**：`{platform}/{id}` 里的 `id` 既认 handle 也认平台的稳定 id
  （YouTube 的 `UC…`）。服务端两边都找；账号上第一次见到 id 会把它记下来，
  下次拿 id 来问就命中。
- **粉丝趋势**：从 WP119c 起本地红人库多一张快照表（`account_observation`，
  只追不删）——每报一次观测就追加一行，report 的趋势取**最近 30 天里最早与
  最近两个快照**的差；快照不足两个就回 `null`（不是编一个 0）。
- **「你已拥有」的邮箱**：`report.tenant_pool.email` 是明文——你自己存的、
  或公共库取过的那条。经配对令牌 + Origin 双校验、只在本机回环上传输，面板
  要把它显示给用户，这是它唯一的存在理由。
- **reveal 的计费与免费重看**：本机已有的邮箱直接给、**0 积分**；本机没有才
  代理云端 reveal，钱扣在云端那侧（`data.kol.lookup`）。取到的邮箱当场写进
  本机加密库与红人池——所以「30 天免费重看」在本机语境下是**永久免费**：
  已经是你自己的了。余额不足回的是 HTTP 200 里的 `payment_required`
  + 一句人话（说清去哪儿充值），不是一张裸的 402。
- **评论文本红线**：`content-observations` 的 schema 是 `.strict()` 的，白名单里
  **没有评论这一格**——`captured_comments` 之类的键一律 400，让「有人想把评论
  塞进公共池」变成一个看得见的信号，而不是被悄悄剥掉。评论只走 `contents`
  （存的是你自己工作区的库），且最多保 200 条（按点赞留最响的）。
- **内容观测上云**（WP129）：`content-observations` 先落本机，**登录了云账号就转发**
  进公共红人库（与红人观测同一条规则，没有第二个开关；没登录一个字节都不出这台
  电脑）。送出去的是窄行：渠道 / 作者 handle / 内容 id / 类型 / 标题 / 发布时间 /
  时长 / 横竖 / 播放 / 点赞 / 评论**数** / 分享 / `paid_promotion`（含付费推广标识）/
  `shoppable`（带货入口）/ 观测时间——**没有**页面地址、封面地址、作者名与粉丝数、
  评论文本、你自己的备注与活动。认不出作者 handle（页面上只有 `UC…` 频道 id）的
  不送。云端按 **渠道 + 内容 id + 观测时间的 UTC 日** 幂等：同一天重复报只刷新数字，
  不多记一行、也不算贡献奖励；所以本机当天去重了也照样转发（上次没送成的这次补上）。
  回执多一格可选的 `forwarded_to_public_library`（1 / 0），老服务不回这一格，当 0 看。
  `paid_promotion` / `shoppable` 是请求体里两个可选的新布尔（只加），没采到就别带——
  `false` 的意思是"看过、没有"。
- **`workbench_url`**：`hello` 的可选新字段，是工作台的基址
  （`http://127.0.0.1:<端口>`）。面板的「去工作台看」拿它当基底拼下面五条
  深链，自己不猜端口：

  | 深链 | 落点 |
  |---|---|
  | `/influencer/creators`（可带 `?creator=<handle|id>`） | 红人面板，自动点开那一个人 |
  | `/influencer/setup` | 红人面板的「导入与 campaign」 |
  | `/settings/credits` | 设置页的「账号与积分」 |
  | `/settings/billing` | 设置页的「账号与积分」 |
  | `/settings/apikeys` | 设置页的「账号与积分」 |

## 11. WP130：列表页批量采集的来源标注 + 红人观测真的进了公共库

**只加不改**。两件事。

### 11.1 观测多三格可选的「列表来源」

插件在列表页（YouTube 搜索结果、视频页右栏「相关视频」、TikTok / Instagram 的搜索与
hashtag 页）上勾一批人收进来时，每一行多带三格（`POST /v1/extension/observations`，
都可选，老插件不带照旧）：

| 字段 | 取值 | 意思 |
|---|---|---|
| `source_page` | `search` / `watch_related` / `hashtag` | 从哪种列表来 |
| `source_query` | 字符串（≤ 600） | 读自哪里：YouTube 搜索页是搜索词，其余是列表页网址 |
| `relevance_score` | 0–100 整数 | 相关视频栏的预筛分；**没打分就不带**（「没法判」不是 0 分） |

它们只记在本机的粉丝快照上（`account_observation`，同时记下这次的 `source`），
**不出本机**——公共库那条窄行里没有它们。

兼容：老的本机服务是 `.strict()` 的，多这三格会整批 400。插件遇到这种 400 会**摘掉
这三格重发一次**，红人照样进库，只是没有来源标注。

### 11.2 修：本机转发的红人观测一条都没进过公共库

WP129 发现的 bug：本机转发红人观测时只送「粉丝数 + 观测时间」，而云端要求「近 30 天
发布数」「互动率」两格必须是数——于是每一条都被拒，回执里的「共享了几条」一直是 0。

修法：

- 本机那一跳在请求体顶上带 `via: 'extension'`（公共库 `POST /v1/data/kol/creators/{channel}/{handle}/observations`
  的一个可选新键）。云端据此把来源记成 `plugin`（与 WP129 内容观测同一口径），并对
  **插件来源**放宽这两格为可选；工作区手填、官方数据接口照旧必须给。
- **缺不是 0**：k-匿名基准跳过缺格的行（也不让它凑够 20 个样本）；体检不拿卡上垫的 0
  下判断（不标「近 30 天没发」、不出互动率分位）。公共库的 sqlite 档不改老表：缺的那格
  在主表里垫 0，真相记在旁表 `kol_observation_gaps`，读出来就是「没有这一格」。
- 本机有值就送、没有就不送（互动率本机库里有就带上；发布数本机没有，不送）。
- 云端收不下的不送，回执才是真话：认不出 handle（只有 YouTube 频道 id `UC…`）的不送；
  没有粉丝数的（列表页只有页面原文）不送——这种行只进本机。
