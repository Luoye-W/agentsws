node_modules/
dist/
.env
.env.*
# WP110：样板文件里全是空值 + 注释，要进仓库（真 `.env` 仍被上面两行挡着）
!.env.example
*.log
.DS_Store
.agentsws-local/
*.tsbuildinfo
.agentsws-local/
# compose 默认的数据目录（NAS 上换成共享目录，见 deploy/nas/README.md）
.agentsws-data/
coverage/
out/
test-results/
playwright-report/
release/

# 桌面端后台会话的临时工作树
.claude/worktrees/
.playwright-cli/
# WP82：官方 Playwright MCP 服务器把截图 / 快照落在 Agent 的 cwd 下的这个目录里
# （`--output-dir` 的默认值）。我们不指定它，所以它跟着一次运行的 cwd 走。
.playwright-mcp/
.dev.vars

# Kiro 派工产物与本地数据（含真实邮箱的导出）
.data/
REPORT.md
KIRO.log

# WP119：插件构建产物与 WXT 生成的类型（`wxt prepare` 每次重出）
apps/extension/build/
apps/extension/.wxt/

QWEN.log

# wrangler 的本地产物（dry-run / dev 的打包输出）
.wrangler/
