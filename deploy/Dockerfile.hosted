# 客服增值服务的**托管实例**镜像（WP128 / docs/64 §11）：Cloudflare Containers 上跑的
# 那一份 `apps/server`。`apps/cloud-worker/wrangler.toml` 的 `[[containers]]` 指向这里，
# `wrangler deploy` 会自己 build + push（推到 Cloudflare 自己的镜像仓库，不用 Docker Hub）。
#
# 与另外两个镜像的关系（三个各建各的，不共用产物）：
#
# - 仓库根的 `Dockerfile`：本地 / NAS 上的服务进程，**带工作台静态产物**；
# - `deploy/Dockerfile.cloud`：Compose 形态的云侧（`apps/cloud`）；
# - 这一个：**只有服务进程**——托管实例没有人会打开它的工作台（商家用自己电脑上
#   那一份），所以不建工作台、不带 Vite 产物，镜像小一截（镜像上限 = 规格的盘，
#   basic 是 4 GB，见 docs/64 §11）。
#
# 做法照抄根 Dockerfile 的三条（理由也一样，别改）：两个阶段都不跑 apt；用
# `pnpm deploy` 而不是 `prune --prod`；不用 root 跑。再加三条这个镜像特有的：
#
# 1. **`linux/amd64`**：Cloudflare Containers 只跑 amd64。在 Apple 芯片上 build 会走
#    模拟，慢，但产物是对的；
# 2. **镜像里没有任何工作区号、任何密钥**。一个镜像给所有订阅的工作区用，
#    区别全在启动时的环境变量（`@agentsws/hosted` 的 `buildHostedEnv`，工作区号只出现
#    在 `AGENTSWS_WORKSPACE_ID` 一处）——将来改成「一个容器托管 N 个工作区」时镜像不动；
# 3. **没有 `VOLUME`**：容器的盘是临时的（官方：All disk is ephemeral），数据靠
#    `apps/server` 自己定时推快照回云端、起来先拉回来（`apps/server/src/hosted-mode.ts`）。

# ── 编译 ────────────────────────────────────────────────────────────────
FROM --platform=linux/amd64 node:22-bookworm AS build
WORKDIR /app

ENV ELECTRON_SKIP_BINARY_DOWNLOAD=1 \
    npm_config_update_notifier=false \
    CI=1

RUN corepack enable

COPY package.json pnpm-lock.yaml pnpm-workspace.yaml ./
COPY tsconfig.json tsconfig.base.json ./
COPY apps ./apps
COPY packages ./packages
COPY role-packs ./role-packs
COPY scripts ./scripts

RUN pnpm install --frozen-lockfile

# 只建服务进程那条引用链（工作台一个字节都用不到）
RUN pnpm exec tsc -b apps/server

RUN pnpm --filter @agentsws/server deploy --prod --legacy /out

# ── 运行 ────────────────────────────────────────────────────────────────
FROM --platform=linux/amd64 node:22-bookworm-slim AS runtime
WORKDIR /app

# 这几个是**镜像级**的缺省；工作区号、令牌、库密钥全在启动时由 HostedInstanceDO 给
ENV NODE_ENV=production \
    AGENTSWS_BIND_HOST=0.0.0.0 \
    AGENTSWS_PORT=4317 \
    AGENTSWS_DATA_DIR=/data

COPY --from=build /out ./

RUN mkdir -p /data && chown node:node /data
EXPOSE 4317

USER node

CMD ["node", "dist/index.js"]
